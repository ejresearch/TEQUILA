# Latin A Curriculum System - Environment Configuration

# LLM Provider Configuration
# Choose provider: "openai" or "anthropic"
PROVIDER=openai

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=your_openai_api_key_here
# MODEL_NAME examples: gpt-4o, gpt-4-turbo, gpt-3.5-turbo

# Anthropic Configuration (if using Anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# MODEL_NAME examples: claude-3-5-sonnet-latest, claude-3-opus-latest

# Model Configuration
MODEL_NAME=gpt-4o
GEN_TEMP=0.25
GEN_MAX_TOKENS=3000
TIMEOUT_S=30

# API Server Configuration
# api_host=0.0.0.0
# api_port=8000
# debug=True

# API Authentication (optional but HIGHLY recommended for production)
# Set this to protect LLM generation endpoints from unauthorized access
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# API_AUTH_KEY=your_secure_api_key_here
