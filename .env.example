# TEQUILA: AI Latin A Curriculum Generator (v1.0 Pilot)
# Environment Configuration - OpenAI GPT-4o Only

# ============================================================================
# REQUIRED: OpenAI API Configuration
# ============================================================================
OPENAI_API_KEY=your_openai_api_key_here

# Model Configuration (OpenAI GPT-4o only)
MODEL_NAME=gpt-4o
GEN_TEMP=0.25
GEN_MAX_TOKENS=3000
TIMEOUT_S=30

# ============================================================================
# OPTIONAL: Cost Tracking (disabled by default)
# ============================================================================
# DRY_RUN=true                   # Test without API calls (no costs)
# BUDGET_USD=100.00              # Optional budget tracking (not enforced)
# COST_WARN_PCT=0.8              # Warn at 80% of budget

# ============================================================================
# OPTIONAL: API Server Configuration
# ============================================================================
# api_host=0.0.0.0
# api_port=8000
# debug=True

# API Authentication (HIGHLY recommended for production)
# Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# API_AUTH_KEY=your_secure_api_key_here

# ============================================================================
# GENERATION PARAMETERS
# ============================================================================
# MAX_WEEK=35                    # Latin A v1.0 Pilot: 35 weeks total
# PROMPT_VERSION=v1              # Prompt version to use
